# 导航与路由重构规划方案（含达妮娅状态标注）

> 状态：**规划稿（待确认）**。本文档只做方案设计，不改动任何源码；确认后另立任务执行。
> **执行状态（2026-08-03）**：已按用户批准执行 P0–P3 —— 达妮娅状态标注（身份=星炬学院学生/状态=失踪/未构成威胁）+ 品牌去「飞行雪绒」化（顶栏→星炬学院档案）、`#characters`→`#fanwork` 改名、一级导航「角色」→「角色档案」、移动端管理员入口（长按 800ms + 页内 modal 替代页脚双击与 prompt）。代码改动详见 git diff；「论坛为主体、飞行雪绒降子模块」结构性重构**未做**（属 ARCHITECTURE-FORUM-PLAN.md 的 P2–P5 独立大项）。
> 配套前置分析：`ARCHITECTURE-FORUM-PLAN.md`（论坛为主体 + 飞行雪绒为子模块的可行性）。
> 工作目录（权威源）：`C:/Users/lenovo/CURSOR/Snow/`

---

## 0. 背景与本次目标

当前站点 `index.html` 是一个**单页 hash 锚点应用（SPA-lite）**，承载「飞行雪绒」——爱弥斯的个人音乐社交空间。导航、角色专题、社区都在同一页面内通过 `#section` 锚点滚动切换；角色深度页（爱弥斯、达妮娅）是**独立的多页 HTML 子站点**（`characters/aimisi/`、`characters/denia/`），通过 `<a href="../index.html#character-hub">` 返回主页。

本次需求拆为两块：

1. **达妮娅状态标注（内容决策）**：确认达妮娅「失踪」，且「未对星炬学院构成威胁」；将其**身份**设定为「星炬学院学生」，**状态**标记为「失踪」。
2. **导航 / 路由模块规划（架构决策）**：厘清整体架构、路由结构、导航层级与边界逻辑，杜绝「世界观角色反向渗透进飞行雪绒个人空间」的违和。

两块的关系：达妮娅的状态标注发生在她的角色深度页，而该页正是导航/路由体系的一员，因此其落点纳入本方案 §5，与导航重构一并规划。

---

## 1. 整体架构设计

### 1.1 当前架构（现状）

```
飞行雪绒站点 (index.html, 单页 hash-SPA)
├── #hero            首屏（爱弥斯身份 + 地点选择器 + 三按钮）
├── #profile         资料（爱弥斯档案）
├── #character-hub   角色专题 ← 链接到 characters/aimisi、characters/denia（独立子页）
├── #music           飞行雪绒音乐空间
├── #characters      角色同人 ← 社区按角色筛选（data-character-tag）
├── #timeline        动态时间线
├── #diary           日志
├── #easter-egg      彩蛋
├── #submit          投稿
├── #community       社区论坛（含分页/筛选/举报/管理 tab）
└── #admin           管理后台（隐藏，口令激活）

子站点：
└── characters/
    ├── aimisi/index.html   爱弥斯深度页（返回 #character-hub）
    └── denia/index.html    达妮娅深度页（返回 #character-hub）
```

### 1.2 已确认的问题点（现状缺陷）

| 编号 | 问题 | 证据 | 影响 |
|---|---|---|---|
| N1 | **两块「角色」语义混淆**：`#character-hub`（line 465，角色专题，跳独立页）与 `#characters`（line 633，角色同人，社区筛选）命名相近但职责不同 | `index.html:465` vs `index.html:633` | 用户/搜索引擎分不清「角色档案」与「角色同人」 |
| N2 | **达妮娅状态缺失**：深度页仅有「所属：残星会 / 星炬学院」和「归于尽」叙事，无显式「失踪」状态标记；且「残星会」在前，「星炬学院学生」身份被弱化 | `characters/denia/index.html:58,71` | 与本次「身份=星炬学院学生、状态=失踪、不构成威胁」诉求不符 |
| N3 | **边界模糊**：角色深度页顶栏品牌写「飞行雪绒」（`denia/index.html:39`），残星会角色因此被裹挟进爱弥斯个人空间品牌下 | `characters/denia/index.html:38-42` | 与「论坛为主体、飞行雪绒为子模块」方向相悖（见前置分析） |
| N4 | **移动端管理员入口脆弱**：依赖页脚双击 + `prompt()`，触屏不可用（前轮已识别为 D2，尚未落地） | `admin-auth.js` | 移动端无法进入管理 |

### 1.3 目标架构（方向，呼应前置分析）

以「**内容域**」而非「单页 section」组织，明确三域边界：

```
站点总底盘 (index.html)
├── 域 A：飞行雪绒个人空间   ← 爱弥斯的号（profile / music / timeline / diary / easter-egg）
├── 域 B：世界观档案 Archive  ← character-hub（角色深度页入口）+ 后续角色档案
├── 域 C：社区大盘 Community   ← submit / community（含 characters 同人筛选）
└── 域 D：系统（account / admin / theme）

导航主线：首页 → [飞行雪绒 | 角色档案 | 社区] 三大入口 → 各自子页面
```

关键点：**达妮娅属于域 B（世界观档案）+ 域 C（社区同人），不属于域 A（飞行雪绒个人空间）**。她的角色页顶栏品牌应从「飞行雪绒」改为中性「星炬学院档案」或站点总品牌，解除「残星会角色出现在爱弥斯个人号」的违和。

---

## 2. 页面路由结构

### 2.1 站点地图（现状 → 目标）

| 路径 | 类型 | 用途 | 所属域 | 鉴权 | 备注 |
|---|---|---|---|---|---|
| `/` (`index.html`) | 单页 hash-SPA | 总底盘 + 域 A/B/C 入口 | 总底盘 | 公开 | hash 锚点切换 section |
| `#hero` / `#profile` / `#music` / `#timeline` / `#diary` / `#easter-egg` | 锚点 | 域 A 内部 | A 飞行雪绒 | 公开 | — |
| `#character-hub` | 锚点 | 角色深度页入口 | B 档案 | 公开 | 需与 `#characters` 区分命名 |
| `characters/aimisi/index.html` | 独立子页 | 爱弥斯档案 | B/A 交界 | 公开 | 返回 `#character-hub` |
| `characters/denia/index.html` | 独立子页 | **达妮娅档案（本次改）** | B 档案 | 公开 | 返回 `#character-hub` |
| `#submit` / `#community` | 锚点 | 域 C 社区 | C 社区 | 匿名可写 | 含 `data-character-tag` 筛选 |
| `#characters`（同人筛选） | 锚点 | 社区按角色过滤 | C 社区 | 公开 | **建议改名** `#fanwork` 消除 N1 |
| `#account` / `#admin` | 状态/隐藏面板 | 账号 / 管理 | D 系统 | 口令/匿名 | 移动端需补入口（N4） |

### 2.2 路由范式约束

- **不使用 SPA 框架 / 构建工具**：站点纯静态，GitHub Pages 直接托管。路由 = 原生锚点（`scrollIntoView`）+ 独立 HTML 子页。
- **深链**：`main.js:3044 handleDeepLinks()` 目前仅处理 `#collection-N`（公开合集）。后续若需角色深链（如 `#character-denia`），应在此统一注册，避免散落。
- **返回锚点统一**：所有角色子页返回一律指向 `#character-hub`（现有已实现，`denia/index.html:39,63`），重构时保持。

---

## 3. 导航层级划分

### 3.1 一级导航（顶部 `nav-glass`，`index.html:82-140`）

现有 7 个入口 + 1 个隐藏管理：
`资料 / 音乐 / 动态 / 日志 / 投稿 / 社区 / 角色`（+ 管理）

**规划调整**：将「角色」明确为「角色档案」（对应 `#character-hub`，域 B），与「社区」下的「角色同人」（`#characters`）在文案上拉开距离，解决 N1。

### 3.2 二级导航

- **角色子页内** `zone-switch`（`denia/index.html:43-48`）：爱弥斯 / 达妮娅 / 西格莉卡(锁定) / 漂泊者(锁定)。
- **社区内** 角色筛选 chips（`#characters` 的 `data-character-tag` 按钮）：爱弥斯 / 达妮娅 / 西格莉卡 / 漂泊者。

### 3.3 面包屑

角色子页：`首页 / 角色 / 达妮娅`（`denia/index.html:62-64`），锚点指向 `#character-hub`。保持。

### 3.4 移动端

- 现有 `menu-btn` 汉堡菜单（`index.html:232`，`main.js:48-51`）控制 `.nav-links.active`。
- **缺口 N4**：管理员入口（页脚双击 + `prompt`）在触屏不可用 → 规划在 §6 P3 补「长按 800ms + 页内 modal」的通用管理员入口，移动端/桌面端统一。

---

## 4. 边界处理逻辑（核心）

### 4.1 内容归属矩阵（落地 N3）

| 内容 | 域 A 飞行雪绒 | 域 B 档案 | 域 C 社区 |
|---|---|---|---|
| 爱弥斯个人动态/日志/音乐 | ✅ 主属 | ◐ 跨链 | ✅ 可投稿 |
| 达妮娅等世界观角色深度页 | ❌ **不放入** | ✅ 主属 | ✅ 可投稿/同人 |
| 残星会阵营相关内容 | ❌ | ✅ 标注「立场存疑」 | ✅ 仅作为考据 |
| 社区 UGC（评论/投稿） | ✅ 可展现 | ◐ 角色标签 | ✅ 主属 |

**规则**：域 A（飞行雪绒个人空间）的 feed / 推荐 / 首屏**不得**注入达妮娅等世界观角色内容；角色内容只在域 B、域 C 出现。

### 4.2 反向渗透防护（guard）

- **首页推荐**：`#hero` 与 `#profile` 附近的「相关角色/好友」区块（若有）只引用爱弥斯官方关系网（诺娃、埃拉拉、塞莱斯特、洛瑟菈），不引入达妮娅。
- **社区筛选**：`#characters` 的同人筛选合法（域 C）；但「社区最新/热门」挂回域 A 首屏属违规，需在渲染层加 `domain` 标记过滤。
- **角色页品牌**：达妮娅/西格莉卡等子页顶栏品牌去「飞行雪绒」化（N3），改为「星炬学院档案」或站点总品牌，从视觉上切断「残星会角色 ∈ 爱弥斯个人号」的暗示。

### 4.3 状态标注的边界（针对达妮娅）

- 「失踪」是**档案属性**（域 B 元数据），**不影响**社区投稿/同步/鉴权逻辑。
- 状态标注只改展示层（`characters/denia/index.html` 文案 + 徽标），不写入 `comments` / `submissions` 表，不触动 Supabase schema。
- 「未对星炬学院构成威胁」作为档案说明文案，与既有「残星会双重身份」叙事并存（不删改官方设定，仅补充项目侧状态判定）。

### 4.4 路由边界

- 角色子页返回锚点统一 `#character-hub`，不回 `#hero` 或 `#profile`（避免把角色页误挂到域 A）。
- 若新增 `#character-<name>` 深链，必须在 `handleDeepLinks` 注册并映射到 `#character-hub` 对应卡片滚动。

---

## 5. 达妮娅状态标注（具体改动，待确认 / 可先行）

> 文件：`characters/denia/index.html`
> 性质：纯文案 + 展示层，**零数据库 / 零 schema 改动**，低风险。

### 5.1 改动点

| 位置 | 现状 | 规划后 |
|---|---|---|
| Hero tagline (`denia:58`) | 「拉海洛的「无害好学生」 · 残星会与星炬学院的双重身份。」 | 「星炬学院学生 · 目前**失踪** · 未对学院构成威胁」（保留「残星会」在背景故事中作为官方设定，不在主标弱化其存在，但主身份前置于星炬学院学生） |
| Stats 新增字段（在 `denia:67-74` 的 `.zone-stats` 内） | — | 新增「**身份**」= 星炬学院学生；新增「**状态**」= 失踪（建议带视觉徽标，如灰色/蓝紫「失踪」tag） |
| Stats「所属」(`denia:71`) | 「残星会 / 星炬学院」 | 保留（官方设定），但在「状态」字段与新 tagline 中将「星炬学院学生」作为首要身份 |
| 顶部返回品牌 (`denia:39,41`) | 「飞行雪绒」 | 改为「星炬学院档案」或站点总品牌（解 N3） |
| 背景叙事 | 含「归于尽 / 放逐虚质空间」 | **保留不改**（官方剧情），仅通过状态字段表达项目侧「失踪」判定，不冲突 |

### 5.2 展示建议

- 在 `.zone-hero` 或 `.zone-stats` 处加一枚 `status-badge`：「🕯 失踪 · 星炬学院学生」，CSS 用低调蓝紫/灰，不与主品牌粉色抢视觉。
- 在背景故事「暗面事件 / 归于尽」模块之前或之后，加一行档案注记：「本档案标注：达妮娅已确认失踪，现有情报未显示其对星炬学院构成直接威胁。」——明确项目侧口径。

### 5.3 是否先行应用

此项为**独立低风险文案改动**，不依赖 §6 的导航重构。可在规划整体确认**之前**单独落地（单独 commit）。是否现在就应用，待你确认时说明即可。

---

## 6. 实施阶段（P0 ~ P3）

| 阶段 | 内容 | 风险 | 依赖 |
|---|---|---|---|
| **P0** | 达妮娅状态标注（§5）；`#characters` 改名为 `#fanwork` + 文案「角色同人」→ 与「角色档案」区分（N1） | 低 | 无 |
| **P1** | 导航层级规范化：一级导航「角色」→「角色档案」；面包屑/返回锚点复核 | 低 | P0 |
| **P2** | 边界 guard 实现：首页/域 A 推荐层加 `domain` 过滤；角色子页品牌去「飞行雪绒」化（N3） | 中 | P1 |
| **P3** | 移动端管理员入口（长按 800ms + 页内 modal 替代页脚双击 + prompt）（N4） | 低 | 无 |

> 注：将「飞行雪绒降级为论坛子模块」「达妮娅页迁入论坛角色档案」等**结构性重构**属于前置分析 `ARCHITECTURE-FORUM-PLAN.md` 的 P2~P5，不在本次导航规划强制范围；若你要推进，需单独立项。

---

## 7. 待你确认的事项

1. **达妮娅状态是否现在先行应用**（§5 可独立落地）？还是随整体规划一起？
2. **导航重构范围**：本次只做 P0（命名澄清 + 达妮娅标注），还是推进到 P1~P3（含品牌去飞行雪绒化）？
3. **角色子页品牌**（N3）：同意把达妮娅页顶栏从「飞行雪绒」改为「星炬学院档案」吗？
4. **`#characters` → `#fanwork` 改名**：同意吗？（影响锚点，需同步改 `main.js` 引用与任何深链）
5. **是否推进**前置分析中「论坛为主体、飞行雪绒为子模块」的结构性重构（独立大项）？

---

## 8. 风险与回滚

- **锚点改名风险**：`#characters` 若改名，须全局搜 `characters`（不含 `characters/` 子目录）确认 `main.js` 内所有 `location.hash` / `scrollIntoView` 引用同步，否则社区筛选失效。回滚：保留旧 id 作别名（`id="fanwork"` 同时保留 `id="characters"` 过渡期）。
- **品牌改动风险**：角色子页去「飞行雪绒」化若误伤爱弥斯页（`characters/aimisi/`），需逐页核对。回滚：仅改 `denia` 页。
- **状态标注零风险**：纯展示层，随时可撤销。

---

*本方案基于 `index.html`、`characters/denia/index.html`、`js/main.js` 实地核查，引用行号均为当前 `C:/Users/lenovo/CURSOR/Snow/` 工作副本。实施前请确认 §7 五项，再由 GitHub Desktop 推送（沙箱不代 push）。*
