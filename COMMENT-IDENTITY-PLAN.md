# 评论身份优化方案（审阅稿 · 未执行）

> 状态：**方案待确认**，尚未改动任何代码。
> 关联：009 号问题（Storage 桶迁移 `migration-009`）已通过测试，本方案不依赖其未生效。

---

## 一、现状与根因

### 1.1 当前昵称逻辑的三个硬编码点
| 位置 | 现状 | 问题 |
|------|------|------|
| `js/auth-manager.js:258` `registerUser` → `signUp({email,password})` | 注册不采集昵称 | 新注册用户 `profiles.nickname` 回落默认 `'匿名信号源'` |
| `js/main.js:2082` / `3273`（动态评论）、`2721`（投稿） | 昵称 `<input … required>` | 对所有身份（含已登录）强制手填 |
| `js/main.js:968-992` `applyProfileToForms` | 仅"预填"，且误把正文框也填成昵称 | 未豁免已登录用户；默认昵称时直接跳过 |

### 1.2 为什么"已登录还要输昵称"
代码里**没有任何「已登录 ⇒ 使用登录身份」的分支**。昵称框一律 `required` 文本输入。即便用户登录且单独设过昵称，也仅是被预填，仍需看到并确认输入框。而绝大多数注册用户从未设过昵称（注册环节没采集），其昵称就是默认的 `'匿名信号源'`，等同于"无有效昵称"，于是评论时必须手输。

### 1.3 匿名用户的痛点
匿名评论只能自由输入昵称，易出现：
- 重复/无意义昵称（"1""a""匿名"）
- 与爱弥斯世界观割裂的称呼
- 每次都要手敲

---

## 二、预设身份方案（匿名用户替代自由输入）

### 2.1 可行性结论：**可行，且零数据库改动**
预设身份本质是"一组受控的昵称候选值"，直接写入现有 `comments.name` / `submissions` 的 `author` 字段即可，**无需新增表/列**。头像配色用预设身份绑定的主题色（复用现有 `color` 字段）。

### 2.2 预设身份清单（鸣潮世界观 + 飞行雪绒设定）
复用 v4 地点选择器已有的 5 个官方地点，并补充用户提议的"罗伊族居民"：

| 身份标签 | 绑定主题色 | 说明 |
|----------|-----------|------|
| 星炬学院学生 | `#FFD700`（金） | 星炬学院 |
| 拉贝尔学部学员 | `#A8D8FF`（浅蓝） | 拉贝尔学部 |
| 拉海洛旅人 | `#6B8AFF`（蓝） | 拉海洛 |
| 雪原守望者 | `#FFB6D9`（浅粉） | 雪原小屋 |
| 电子海漂流者 | `#B66BFF`（紫） | 电子海 |
| 罗伊族居民 | `#FF6B9D`（粉） | 用户提议 |
| 匿名信号源 | `#FFFFFF`（白） | 兜底默认（保留现有语义） |

### 2.3 交互形态（建议）
- 匿名用户：昵称框替换为**预设身份下拉 `<select>` + 「自定义」展开项**。默认选中「匿名信号源」或第一项；选预设即用之，选「自定义」再出现文本框。
- 已登录且昵称为默认/空：同样显示预设身份选择器（或提示先完善昵称）。
- 已登录且有有效昵称：**隐藏昵称控件**，显示「以 _[昵称]_ 身份发表」（提供「改用预设/自定义」折叠入口，保持灵活）。

---

## 三、已登录用户免昵称输入方案

### 3.1 核心：新增统一身份解析函数
在 `js/auth-manager.js` 新增：

```js
// 返回当前评论应使用的"有效昵称"；无效（默认/空）返回 null
function getEffectiveNickname() {
    var n = (session && session.nickname) || '';
    if (n && n !== '匿名信号源') return n;
    return null;
}
```

- 已登录 + 有效昵称 → 返回该昵称，**UI 隐藏昵称框**，提交时直接用。
- 已登录但无效 / 匿名 → 返回 `null`，UI 显示预设身份选择器。

### 3.2 预设身份常量集中管理（新增 `js/identities.js`）
```js
window.FXRE_IDENTITIES = [
  { label: '星炬学院学生',   color: '#FFD700' },
  { label: '拉贝尔学部学员', color: '#A8D8FF' },
  { label: '拉海洛旅人',     color: '#6B8AFF' },
  { label: '雪原守望者',     color: '#FFB6D9' },
  { label: '电子海漂流者',   color: '#B66BFF' },
  { label: '罗伊族居民',     color: '#FF6B9D' },
  { label: '匿名信号源',     color: '#FFFFFF' }
];
```

---

## 四、具体修改点（行号级）

| 文件 | 位置 | 改动 |
|------|------|------|
| `js/identities.js` | 新增 | 预设身份常量 + 配色 |
| `js/auth-manager.js` | 新增 ~262 后 | `getEffectiveNickname()`；导出到公开 API |
| `js/main.js:2082` | `buildCommentAreaHTML` 昵称输入模板 | 改为动态身份控件（按 `getEffectiveNickname()` 决定隐藏还是下拉）|
| `js/main.js:3273` | 社区评论表单模板 | 同上 |
| `js/main.js:2721` | 投稿 `submit-nickname` | 同上（投稿区昵称控件改造）|
| `js/main.js:2387-2482` | `handleCommentSubmit` | 昵称取值改为：`getEffectiveNickname()` → 预设选择器值 → 自定义值；去 `required` 强依赖 |
| `js/main.js:3483-3531` | 社区评论提交 | 同上取值逻辑 |
| `js/main.js:2719-2754` | 投稿提交 | 同上 |
| `js/main.js:968-992` | `applyProfileToForms` | **修复误填正文框 bug**；改为调用统一身份解析，不再无差别预填 |

> 注：所有昵称最终仍以 `name` / `author` 字段写入，复用现有去重（按 `text+name` 签名）与合并逻辑，**不触碰数据库 schema**。

---

## 五、实施规划（确认后执行顺序）

1. **新增 `js/identities.js`** —— 预设身份常量。
2. **`auth-manager.js` 加 `getEffectiveNickname()`** 并导出。
3. **改造 3 处昵称控件模板**（评论 / 社区评论 / 投稿）为动态身份控件。
4. **改造 3 处提交函数**的昵称取值逻辑（优先登录昵称 → 预设 → 自定义）。
5. **修复 `applyProfileToForms` 误填 bug**。
6. **语法校验**：`node --check` 三个 JS 文件。
7. **本地刷新验证**：分别用「已登录有昵称 / 已登录无昵称 / 匿名」三种身份发表评论，确认昵称框显隐正确、预设身份可选、提交落库无误。
8. **由你 commit + push**（GitHub Desktop）→ GitHub Pages 自动部署。

---

## 六、风险与兼容

- **零数据库变更**：昵称字段复用，预设身份即受控昵称值，去重/合并逻辑无需改。
- **向后兼容**：历史评论的 `name` 不受影响；旧 `匿名信号源` 仍保留为兜底预设。
- **可降级**：若 `getEffectiveNickname()` 缺失，控件退回原文本输入（不阻断提交）。
- **注册昵称采集（可选增强）**：可在 `registerUser` 注册时加昵称字段，让新用户直接拥有有效昵称；属锦上添花，**本方案不强制包含**，可单列一期。

---

## 七、待你确认的事项

1. 预设身份清单是否认可（尤其是否保留「罗伊族居民」、是否增减）？
2. 已登录用户是否**默认隐藏**昵称框（仅显示"以 X 身份发表"），还是保留「可展开自定义」？
3. 投稿区的昵称是否**一并**按此规则改造，还是仅评论区先行？
4. 是否顺带把「注册时采集昵称」纳入本期？

> 确认后我立即动手；按约定**最终 push 由你用 GitHub Desktop 完成**，我不会在沙箱代推。
