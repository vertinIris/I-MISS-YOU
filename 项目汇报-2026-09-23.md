# 飞行雪绒（Snow）项目汇报

> 生成时间：2026-09-23 20:25 · 数据来源：当前代码仓库 + git 历史 + `docs/STATUS.md` + `package.json`

## 1. 基本信息

| 项目 | 值 |
|------|-----|
| 项目名 | flying-edelweiss（飞行雪绒 / Snow） |
| 性质 | 鸣潮角色爱弥斯（Aemeath）同人社交体验站 + 独立论坛「星炬学院」 |
| 当前版本 | **v11.5.1**（`package.json` / `docs/STATUS.md` / 页脚 / `window.__FXRE_API.version`） |
| SW 缓存版本 | 已升至 **v11.5.2**（`snowfluff-v11.5.2-layout-fix`，强制失效旧缓存） |
| 仓库 | `vertiniris/I-MISS-YOU`（GitHub），共 26 个提交 |
| 当前分支 | `main` |
| 最后提交 | 2026-09-11（约 12 天前） |
| 工作树 | **干净**（无未提交改动） |

## 2. 技术架构

| 层级 | 技术 |
|------|------|
| 前端 | HTML5 + CSS3 + 原生 JS（零框架、无打包管道） |
| 后端 | Supabase（PostgreSQL + Auth + Realtime + RLS + Storage） |
| 音频 | Web Audio API |
| 部署 | **GitHub Pages（主，CI 自动）**；README 提及的 Cloudflare Pages（`i-miss-you.pages.dev`）经核实**非本项目**，为陌生页面占用 |

## 3. 代码结构

| 目录 | 内容 | 规模 |
|------|------|------|
| `index.html` | 主站（飞行雪绒）入口 | — |
| `forum/` | 星炬学院论坛：`index.html` + `js/` + 4 个 CSS | 20 文件 |
| `characters/` | 7 角色专区：aimisi / denia / drifter / linne / lucilla / mornye / sigrica | 7 HTML |
| `css/` | tokens + style + 论坛/档案子集 + `edge-compat.css` | 30 个 |
| `js/` | 主站逻辑 + Supabase 适配（auth-manager、supabase-adapter、web-vitals-collector、markdown-renderer…） | 26 个 |
| `db/` | 数据库迁移 `migration-001` … `030` | 33 个 SQL |
| `scripts/` | 冒烟检查 / 极端审计 / 浏览器探针 / 内容构建 | 18 个 |
| `dist/` | 构建产物：`bundle-main.js`(237KB) `bundle-forum.js`(151KB) + 源码映射 | 8 文件 |
| `论坛内容/` | 二创内容库源（→ `npm run content:build` 构建） | 50 文件 |
| `docs/`、`design-docs/` | 状态 / 内容管线 / 世界观 / 设计文档 | 25+ |

文件统计（不含 node_modules）：**JS 59 · CSS 30 · HTML 12 · SQL 33**。

## 4. 关键模块

- **评论 / 投稿系统**：Supabase 云端同步；`migration-017` 由软删改为**物理硬删 + Realtime DELETE**（审计靠 `moderation_logs.content_snapshot`）。
- **双套 Auth**：主站 `js/auth-manager.js` ↔ 论坛 `forum/js/forum-auth.js`，同域会话共享；管理员分别为 `profiles.role` 与 `forum_admins`/`is_forum_admin()`。
- **内容管线**：`论坛内容/` → `npm run content:build` → `forum/js/forum-import-data.js` → Supabase；`type:lore` 档案由构建分流 + `ensureCloudSeed` 白名单双拦。
- **安全加固**：移除 `script-src 'unsafe-inline'`，内联脚本外提；`_headers` 注入 CSP/HSTS/X-Frame-Options 等；`edge-compat.css` 为 `oklch()`/`color-mix()` 提供 `@supports` 回退。
- **监控 / 质量**：`web-vitals` RUM（`migration-030`）；axe-core a11y CI；Lighthouse CI（a11y≥0.9 阻断）。
- **前端增强**：Markdown 渲染（marked + DOMPurify + highlight.js）、PhotoSwipe 灯箱、Just-Validate 表单校验。

## 5. 近期改动（2026-09-09 ~ 09-11，最近 15 次提交）

- **v11.5.1→v11.5.2 布局修复**：分区布局错位、同步指示器挂载区、Hero 滚动旋钮重叠、页脚隔离。
- **陈旧缓存根因修复（fxre）**：S-01 reveal 降级兜底（`@media scripting:none` + 超时自显 + observer 容错）；S-02/S-03 静态资源改 `network-first` + CI 注入部署号强制 SW 更新；M-05 runtime-assert 增 bundle 执行探针。
- **根布局上下文**：`html` 加 `height:100%+overflow-x:hidden`；`body` 加 `padding-top:64px`；新增 `#main-content` 独立布局上下文；关键 CSS 变量加 fallback。
- **CI 自动化**：新增 `pages-deploy.yml`（GitHub Pages 自动部署，2026-09-10 首次运行启用 Pages）；`dist-sync` 加固（锁定 terser/csso 版本、Node 22、加 `--legacy-peer-deps`、排除 source map）。
- **依赖清理**：移除 `ters` 废依赖、修正 `homepage`、对齐 `@eslint/js` 到 v9 消除 peer 冲突。

## 6. 运行状态

- **本地服务**：当前会话**无服务器运行**（8848 端口未监听）——属纯分析/构建会话；可经 `npm run serve`（端口 8848）或 `打开本地预览.bat` 启动。
- **CI 工作流**（`.github/workflows/`）：`static-checks.yml`、`lighthouse.yml`、`accessibility.yml`、`pages-deploy.yml` 共 4 个，均健全。
- **部署**：GitHub Pages 自动部署，最后 push 于 2026-09-11，线上版本 v11.5.x。
- **健康度**：工作树干净；`npm run smoke-check` / `extreme-audit` 为本地门禁（Codex 不可用，以二者 + 落盘评审文档替代双轮评审）。

## 7. 已知问题 / 技术债

1. **文档版本滞后**：`README.md` 仍标注 v11.3.2 / Cloudflare Pages 主站，与实际（v11.5.1 / GitHub Pages 主）不符，需同步。
2. **无打包管道**：暂缓 terser/minify，避免破坏全局与加载顺序。
3. **`main.js` 模块化暂缓**：4564 行 IIFE 共享闭包变量，拆分风险高。
4. **评审闭环受限**：无 Codex / code-review MCP，依赖 `smoke-check` + `extreme-audit` + 文档评审。
5. **部分技能未启用**：见《本地 Skill 安装情况汇报》——71 个本地技能中仅 1 个进入当前会话可用清单。

## 8. 结论

项目处于**稳定维护期**：v11.5.x 功能完整，近两周集中修复了布局错位与陈旧缓存根因，CI 防护（静态检查 / Lighthouse / a11y / 自动部署）健全，部署自动化到位。主要待办为**文档版本同步**与**技术债收口**（打包/模块化）。整体运行健康，无阻断性风险。
