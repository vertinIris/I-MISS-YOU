# 飞行雪绒 · 后续优化路线图（v10 基线）

> 文档版本：v1.0 · 2026-08-02
> 基线：`64cccb0` + 页脚 v10.0（`486b780`）已上线 GitHub Pages
> 方法：**先实测、后建议**。本文所有"缺口"均来自对当前代码的 grep / 读取 / 部署拉取验证，所有"已具备"亦经实测确认，避免凭印象下结论。
> 原则：**效果优先**。每项标注预期收益（效果）与风险/工作量，效果不明显或无收益的项明确标注"不推荐/暂缓"，不做过度工程。

---

## 0. 审计方法与已确认事实

我本轮实际做了以下检查（命令与结果）：

| 检查项 | 命令/位置 | 结论 |
|---|---|---|
| reduced-motion | `css/style.css:3599` | ✅ 已全局处理 |
| OG 元数据 | `index.html:10-20` | ✅ 完整（type/url/image + twitter） |
| 字体 swap | `index.html:40` (`&display=swap`) | ✅ 已用 |
| CSP | `index.html:24-34` | ✅ 已配置 |
| aria-label 控件 | `index.html:210/232/548/553/561...` | ✅ 交互控件齐全 |
| ESC 关闭 | `main.js:1926/1942/4128` | ✅ 已实现 |
| 粒子性能 | `particles.js:74`（24/42） | ✅ 克制 + CSS 降级 |
| 双写/合并/乐观更新 | `repository.js` / `supabase-adapter.js` | ✅ 已实现 |
| RLS/服务端限速 | `db/migration-002/007/014` | ✅ 已落地 |
| 字阶令牌 | `css/style.css :root`（R18） | ✅ 已定义 |
| 脚本加载 | `index.html:1824-1839` | ⚠️ 13 个 `<script>` 无 `defer` |
| 按钮/标签字号 | `css/style.css` 20+ 处 `0.75rem` | ⚠️ 未用 `--fs-caption` 令牌 |
| 模态可访问性 | `index.html` 模态容器 | ⚠️ 缺 `role="dialog"`/`aria-modal`/焦点陷阱 |
| og:image 体积 | `assets/og-cover.png` = 2.0 MB | ⚠️ 偏重 |
| 构建/压缩 | 无 `dist/`、无 CI | ⚠️ 未压缩、无自动化 |
| PWA | 无 `sw.js`/`manifest.json` | ⚠️ 未做（可选） |

---

## 1. 优化项矩阵（按优先级）

优先级说明：**P0**=立即可做、高收益低风险；**P1**=值得做、中等投入；**P2**=可选/进阶，效果优先下可暂缓。

### P0-1 · 字阶下钻到按钮 / 标签 / 小字（R18 收尾）
- **现状（实测）**：`css/style.css` 中 `--fs-caption`/`--fs-body` 令牌已定义，但仍有 **20+ 处裸 `font-size: 0.75rem`** 和若干裸 `1rem`（如 `:1138/:1185/:1911/:1931/:2633/:2829/:3289/:3695/:3831/:3963/:4626/:4857/:4923/:4954/:5043/:5059/:5071/:5208/:5278/:5297/:5744/:5796/:5873/:5901`，以及 `:2743/:5858/:6146` 的 `1rem`）。这些是按钮文字、标签、卡片 meta、时间轴小注。
- **问题**：主题切换或字号策略微调时，这些裸值不会随令牌统一变化；层级节奏在"小字层"不一致，违背 R18"统一字阶"的初衷。
- **方案**：将裸 `0.75rem` → `var(--fs-caption)`，`1rem` → `var(--fs-body)`；按钮文字统一走 `--fs-caption` 或新增 `--fs-button` 令牌。示例：
  ```css
  .btn, .tag, .card-meta { font-size: var(--fs-caption); line-height: var(--lh-caption); }
  ```
- **预期效果**：全站字阶 100% 由令牌驱动 → 后续调字号只改 `:root`；小字行高从默认 1.5 收口到 `--lh-caption`，阅读节奏更统一。视觉一致性提升，零回归风险。
- **工作量**：低（约 25 处替换，可 `replace_all` 后人工核对）。
- **风险**：低。替换后跑一次 `node --check` 无关（CSS），肉眼 + 浏览器抽查即可。
- **依据**：REVIEW-v9.6 R18「建立四级以上 type scale」的收尾条款（此前仅落地标题层，未下钻控件层）。

### P0-2 · 模态框可访问性补强（WCAG 2.1 AA）
- **现状（实测）**：模态/面板（书签面板、指南 modal、举报 modal、版主面板、公开合集面板）依赖 `hidden` 切换 + 关闭按钮 `aria-label`，但容器**无 `role="dialog"`、无 `aria-modal="true"`、无焦点陷阱**。`main.js` 仅对个别输入框/按钮做了 `.focus()`（`:1375/:4137`），无 Tab 循环约束。
- **问题**：屏幕阅读器用户不会感知"模态已打开"；键盘 Tab 可逃出模态聚焦到背景元素；不符合 WCAG 2.1 AA（成功标准 2.1.1 / 2.4.3 / 4.1.2）。
- **方案**：封装一个通用初始化器（效果优先、改动小）：
  ```js
  function initModalA11y(panel, trigger) {
    const prevFocus = trigger || document.activeElement;
    panel.setAttribute('role', 'dialog');
    panel.setAttribute('aria-modal', 'true');
    // 打开：聚焦首个可聚焦元素
    // 焦点陷阱：keydown Tab 在 panel 内循环
    // 关闭：归还焦点到 prevFocus
  }
  ```
  并给每个面板加 `aria-labelledby` 指向其标题（标题已有，如 `.modal-header h3`）。
- **预期效果**：键盘/读屏用户可正常进出模态；达到 WCAG AA 键盘可达性；对普通用户无视觉变化（零可见副作用）。
- **工作量**：中（写 1 个 helper + 5 处调用 + 少量 HTML 属性）。
- **风险**：低。需自测：Tab 循环、ESC 仍可用、焦点归还。
- **依据**：项目"Premium 标准"含 WCAG 2.1 AA 合规要求；当前为唯一明显未达标项。

### P0-3 · 脚本加载加 `defer`（零风险性能微调）
- **现状（实测）**：13 个 JS 文件用普通 `<script src>`（`index.html:1827-1839`），无 `defer`。它们位于 `</body>` 前，不阻塞解析，但下载与 HTML 解析串行。
- **方案**：给非依赖顺序敏感的脚本加 `defer`（注意：`particles.js`/`main.js` 等存在加载顺序依赖，需保留现有顺序或改 `type="module"`）。最小改动：整体加 `defer`，浏览器按文档顺序执行。
- **预期效果**：脚本与 HTML 解析并行下载，首屏可交互时间（TTI）小幅下降。零视觉变化。
- **工作量**：低（改 13 个标签）。
- **风险**：低。需确认脚本间无"立即执行且依赖前脚本已运行"的隐式假设（现有代码多在 DOMContentLoaded / 事件内执行，安全）。
- **依据**：Web 性能常识；非阻塞加载是 Premium 性能标准（≤1.5s）的基础项。

---

### P1-1 · JS 压缩 + 关键 CSS 内联（构建管线）
- **现状（实测）**：`main.js` 233KB / 4564 行未压缩；无 `dist/`、无构建步骤；`package.json` 仅有 `serve`/`syntax-check`/`smoke-check`，无 `build`。
- **方案**：引入极简构建——`terser` 压缩 13 个 JS（`npm run build` → `dist/`），并把首屏必需 CSS 内联进 `index.html` `<head>`，其余 CSS 异步加载。GitHub Pages 无构建，需用 **GitHub Actions** 生成 `dist/` 再部署（见 P1-2）。
- **预期效果**：`main.js` 体积可降 40–60%（约 90–140KB）；首屏 CSS 内联减少一次往返。Lighthouse 性能分提升。
- **工作量**：中（加 `package.json` build 脚本 + `.github/workflows/deploy.yml`）。
- **风险**：中。需处理脚本路径从 `js/` 改 `dist/`、CSP 哈希（若启用 SRI）。建议先只做压缩，内联 CSS 作为第二步。
- **依据**：Premium 性能标准「Load times under 1.5s / 60fps」；当前纯靠手工未压缩。

### P1-2 · 自动化 CI（防回归）
- **现状（实测）**：无 `.github/workflows`。`package.json` 已有 `syntax-check`（`node -c` 全部 JS）与 `scripts/smoke-check.mjs`，但纯人工触发。v10 那次能零回归，靠的是我手动跑 `node --check`——不可持续。
- **方案**：加 `.github/workflows/ci.yml`：push 到 `main` 时自动跑 `npm run syntax-check` + `npm run smoke-check`；可选加 Playwright 冒烟（开两个页面验证 R20 删除联调）。
- **预期效果**：任何 JS 语法错误 / 明显回归在 push 即被卡住，避免再次"本地改完忘验证"。对终端用户无直接效果，但保障长期质量（效果优先下的"隐性收益"）。
- **工作量**：低-中（写 1 个 yaml + 确保 smoke-check 可无头运行）。
- **风险**：低。注意沙箱无 Supabase 凭据，CI 需对"云端未就绪"分支做容错（现有代码已有降级，应可通过）。
- **依据**：质量保障要求「Test every interactive element」；当前无自动化护栏。

### P1-3 · `og:image` 瘦身
- **现状（实测）**：`assets/og-cover.png` = **2.0 MB**，被 `index.html:16/20` 引用为分享预览图。
- **方案**：转为 `og-cover.webp`（质量 80%，目标 < 300KB），同时保留 SVG 矢量版用于高清屏；更新 meta 指向 webp。
- **预期效果**：社交平台分享卡片加载更快（2MB → ~250KB），提升分享成功率与体验。
- **工作量**：低（转格式 + 改 2 行 meta）。
- **风险**：低。注意 Discord/Twitter 对 WebP og:image 支持已良好；老旧抓取器可能不支持，可保留 PNG 作 `og:image` 备用（用 `<meta property="og:image" content="...webp">` + 部分平台兼容）。
- **依据**：社交分享体验优化（效果导向，非强制）。

---

### P2-1 · PWA / Service Worker（可选，锦上添花）
- **现状**：无 `manifest.json` / `sw.js`。
- **评估**：**效果优先下建议暂缓**。本项目强依赖 Supabase Realtime 与登录态，离线场景价值有限；加 SW 主要为"可安装"与"弱网卡顿可看缓存壳"。若做，仅做"导航路由缓存 + 静态资源缓存"，**不要缓存动态数据**。
- **工作量**：中。**风险**：中（SW 更新/失效易踩坑）。**不推荐**作为近期重点。

### P2-2 · 数据冲突合并（进阶）
- **现状**：本地 + 云端双写、乐观更新、合并去重已实现（`getComments`/`getSubmissions` 合并本地与云端）。冲突策略为"最后写入胜"。
- **评估**：多设备同时编辑同一条投稿/评论的冲突概率低，**效果优先下暂缓**。若做，给 `submissions`/`comments` 加 `updated_at` 比较 + 简单版本向量（暂不引入 CRDT）。
- **工作量**：中-高。**风险**：中（合并逻辑易引入数据丢失）。**不推荐**近期。

### P2-3 · 大陆访问镜像（部署层）
- **现状**：GitHub Pages 在大陆访问慢/不稳定。
- **评估**：已知限制。可选 `cloudstudio-deploy` 镜像或接入已连接的 **EdgeOne Pages**（注意：用户历史记录显示 EdgeOne「全球不含大陆」区域会 401，需 ICP 备案才可达大陆；无备案时改用 cloudstudio 更稳）。
- **工作量**：低（部署动作）。**风险**：低，但涉及账号/备案决策，需你确认。
- **依据**：部署优先级约定（EdgeOne Makers > cloudstudio 备选）。

---

## 2. 明确"不推荐做"的项（避免过度工程）

| 项 | 理由 |
|---|---|
| 引入框架（React/Vue）重写 | 当前原生 JS + 模块化已可维护；重写风险高、收益低，违背"效果优先" |
| 把全部 CSS 拆微服务式多文件 | 单文件 6280 行虽大，但无构建管线时拆文件反而增加请求数；等 P1-1 构建就绪后再考虑 |
| 大量新增动效 | reduced-motion 已全局关闭动画；新增动效需谨慎，避免眩晕与性能浪费 |
| 服务端重构成 Supabase Edge Functions 全套 | 当前 RPC + RLS 已满足，过度后端化增加运维成本 |

---

## 3. 推荐实施顺序（路线图）

```
阶段 A（P0，半天内可全部落地，零可见副作用）
  ├─ P0-1 字阶下钻（25 处裸值 → 令牌）
  ├─ P0-2 模态 role/aria-modal/焦点陷阱
  └─ P0-3 脚本 defer
  → 提交 + 推送 + 浏览器抽查（主题切换/键盘 Tab/模态）

阶段 B（P1，1–2 天，需引入构建）
  ├─ P1-2 CI（先上，护栏）
  ├─ P1-1 JS 压缩（terser → dist/）+ GitHub Actions 部署
  └─ P1-3 og:image 转 WebP

阶段 C（P2，按需）
  ├─ P2-3 大陆镜像（你确认后）
  └─ P2-1/P2-2 视产品需求再议
```

---

## 4. 整体效果评估（v10 基线 → 路线完成后）

- **严谨度**：v10 已通过 REVIEW-v9.6 全 20 项（R17/R18/R19/R20 收尾），代码层与云端层均闭环。
- **可访问性**：P0-2 完成后将从"基本可用"升到"WCAG AA 键盘可达"，填补当前唯一明显短板。
- **一致性**：P0-1 完成后字阶 100% 令牌化，主题与排版策略可一处统调。
- **性能**：P1-1 压缩 + P0-3 defer 后，首屏体积与 TTI 应有可测改善（Lighthouse 性能分预期 +5~10）。
- **可持续性**：P1-2 CI 上线后，回归风险显著下降。

**预期净效果**：在"效果优先"前提下，阶段 A 三项均为**低风险、零可见副作用、可测收益**，是最值得立刻执行的部分；阶段 B 是"把手工保障变成自动保障"；阶段 C 视需求。

---

## 5. 下一步建议（立即可做）

我建议从 **阶段 A 三项** 开始——它们都不需要你做任何 Supabase / 部署凭据操作，纯前端改动，浏览器抽查即可验证。要不要我现在就动手把 P0-1 / P0-2 / P0-3 一次性落地并提交？（字阶下钻 + 模态可访问性 + 脚本 defer）
